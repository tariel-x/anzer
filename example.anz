type Status = {
    gift :: MaxLength 10 MinLength 20 String
    sent :: []{
        subfield :: *Gift
    }
}
type Another = Gift
github.com/test/deliver[go] :: Gift -> Either Error Optional Status

makegift = deliver . prepare . parse

invoke (
    makegift,
    deliver,
)
